{
  "name": "pollframe",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1",
    "check": "npm run data:validate && npm run build && npm run security:check",
    "test:browser": "playwright test",
    "data:update": "node scripts/update-poll-data.mjs",
    "data:map": "node scripts/build-state-map-data.mjs",
    "data:validate": "node scripts/validate-poll-data.mjs",
    "security:check": "node scripts/validate-security.mjs"
  },
  "engines": {
    "node": ">=22.12"
  },
  "dependencies": {
    "@svg-maps/germany": "2.0.0",
    "react": "19.2.8",
    "react-dom": "19.2.8"
  },
  "devDependencies": {
    "@playwright/test": "1.62.0",
    "vite": "6.4.3"
  }
}
